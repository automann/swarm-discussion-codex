# Synthesis

Conditionally certify only if this retained artifact tree passes projection-aware gates and preserves the raw real-host transport chain.

Both projected experts agree that the release claim depends on retained, replayable evidence binding projected agent names and hashes to real Codex host ids, prompt-build artifacts, raw wait batches, normalized collection, finalized WAL, trace, evidence, and validators. The skeptic highlights validator substitution and missing raw wait provenance as P0 blockers; the certifier withholds certification until the artifact bundle demonstrates that chain.
