{
  "schemaVersion": 1,
  "kind": "swarm.capability_profile",
  "id": "expert-readonly",
  "title": "Ordinary expert, readonly tools",
  "role": "ordinary-expert",
  "default": false,
  "status": "experimental",
  "allowedTools": ["read", "glob", "grep"],
  "toolEvidencePolicy": {
    "requiresLoggedToolCall": true,
    "requiresValidation": true,
    "citation": "allowed only when a tool-evidence JSONL record is validated"
  },
  "notes": [
    "This retained smoke uses the readonly candidate profile so the certification artifact can record citable, validated coordinator-side contract inspection evidence without granting broad expert tools."
  ]
}
