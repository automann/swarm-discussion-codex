# Synthesis

Recommendation: certify the Codex adapter release only when retained artifacts prove real Codex expert thread transport and every runtime-owned release gate passes.

The certifier and skeptic agree that certification must not rest on simulated or locally reconstructed transport. The retained release-smoke bar should include runtime prompt-build artifacts, real Codex host ids for `certifier` and `skeptic`, preserved raw host read batches before normalization, normalized transport collection, finalized WAL state, trace, evidence, adapter-smoke, validate-loop, validate-discussion, and certify_adapter output.

The main nuance is gate labeling: the skeptic would keep the P0 certification blockers explicit so the gate remains rigorous without turning every diagnostic into ceremony. For this retained-adapter certification run, the P0 claim is real-host provenance, so the retained chain is required.

Message anchors: `r1-msg-001`, `r1-msg-002`, `r1-msg-003`.
